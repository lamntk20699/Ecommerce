export const productSelector = (state) => state.products;

export const cartSelector = (state) => state.cart;

export const meSelector = (state) => state.userInfo;

export const orderSelector = (state) => state.orderList;
